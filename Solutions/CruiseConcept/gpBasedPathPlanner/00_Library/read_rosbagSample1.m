% Sample script to demonstrate execution of function topics = read_rosbag(filePath, savePath)
filePath = []; % Initialize filePath here
savePath = []; % Initialize savePath here
topics = read_rosbag(filePath, savePath);
